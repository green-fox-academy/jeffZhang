const { car, computer } = require('./objects')
let car_model = car.model,
  car_color = car.color,
  car_year = car.year,
  car_doors = car.doors,
  car_historical = car.historical

let computer_type = computer.type,
  monitor_color = computer.monitor.color,
  monitor_size = computer.monitor.size,
  monitor_HDMI = computer.monitor.HDMI,
  monitor_VGA = computer.monitor.VGA,
  tower_color = computer.tower.color,
  tower_CPU = computer.tower.CPU,
  tower_memory = computer.tower.memory,
  tower_SSD = computer.tower.SSD
